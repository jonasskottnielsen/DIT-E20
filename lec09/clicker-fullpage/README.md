Clicker game with full page reload
----------------------------------

This folder contains a clicker server performing a full page reload.

The [clicker-server.js](clicker-server.js) responds to a POST request with an updated HTML page.

To run:
```
  $ npm install
  $ node clicker-server.js
```

To view: open http://localhost:8080 in your browser.
